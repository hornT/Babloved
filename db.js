const config = require('./config');


client.on("error", function(error) {
    console.error(error);
  });

function getRate(){
}

function setRate(rate){
}

module.exports = {
    getRate,
    setRate,
}